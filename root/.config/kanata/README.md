1. Paste the kanata.kbd in .config/kanata/ with the Kanata binary
2. Paste the kanata.service in .config/systemd/user/
3. For running kanata at startup run this command: systemctl --user enable kanata
